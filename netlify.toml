[build]
  publish = "_site"  # Quarto output folder
  command = "apt-get update && apt-get install -y r-base && quarto render"

[build.environment]
  NODE_VERSION = "18"

[[plugins]]
  package = "@quarto/netlify-plugin-quarto"